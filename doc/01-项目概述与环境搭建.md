# 第01课：项目概述与环境搭建

## 🎯 本课目标

- 了解 AI Agent 平台的功能和架构
- 掌握 Python 虚拟环境的创建与使用
- 成功运行项目并访问 API 文档

---

## 📖 什么是 AI Agent 平台？

### 1.1 功能介绍

这是一个 **AI 智能体对话平台**，核心功能包括：

| 功能模块 | 说明 |
|---------|------|
| 智能体管理 | 创建、编辑、删除 AI 智能体，每个智能体有独特的人设（System Prompt）|
| 对话管理 | 与智能体进行多轮对话，保存对话历史 |
| LLM 集成 | 对接通义千问/OpenAI 等大语言模型 API |

### 1.2 技术栈

```
后端技术:
  ├── FastAPI      - Python Web 框架（高性能、自动生成文档）
  ├── SQLAlchemy   - ORM 框架（对象关系映射）
  ├── Pydantic     - 数据验证库
  ├── httpx        - 异步 HTTP 客户端
  └── SQLite       - 轻量级数据库

前端技术:
  ├── HTML/CSS/JavaScript
  └── 原生 Fetch API
```

---

## 🏗️ 项目架构

```
┌─────────────────────────────────────────────────────┐
│                    前端 (静态页面)                    │
└──────────────────────────┬──────────────────────────┘
                           │ HTTP/REST API
                           ▼
┌─────────────────────────────────────────────────────┐
│              FastAPI 后端                            │
│  ┌─────────────────────────────────────────────┐    │
│  │         Routers (路由层/API接口)             │    │
│  └─────────────────────────────────────────────┘    │
│                        │                             │
│  ┌─────────────────────────────────────────────┐    │
│  │         Services (服务层/业务逻辑)           │    │
│  └─────────────────────────────────────────────┘    │
│                        │                             │
│  ┌─────────────────────────────────────────────┐    │
│  │       Repositories (仓库层/数据访问)         │    │
│  └─────────────────────────────────────────────┘    │
└───────────────────────┬─────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        ▼                               ▼
   ┌─────────┐                  ┌─────────────┐
   │ SQLite  │                  │   LLM API   │
   │ 数据库   │                  │  (通义千问)  │
   └─────────┘                  └─────────────┘
```

### 💡 分层架构的好处

| 层级 | 职责 | 好处 |
|------|------|------|
| **Router** | 接收 HTTP 请求，返回响应 | 专注于 API 定义 |
| **Service** | 业务逻辑处理 | 逻辑复用，易于测试 |
| **Repository** | 数据库 CRUD 操作 | 数据访问解耦 |

---

## 🛠️ 环境搭建

### 步骤 1：检查 Python 版本

```bash
python --version
# 要求: Python 3.10 或更高版本
```

### 步骤 2：创建虚拟环境

```bash
# 进入项目目录
cd AI_Agent_Platform

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# macOS/Linux:
source venv/bin/activate

# Windows:
venv\Scripts\activate
```

> 💡 **什么是虚拟环境？**  
> 虚拟环境是一个独立的 Python 环境，项目依赖的包只安装在这个环境中，不会影响系统全局的 Python。

### 步骤 3：安装依赖

```bash
cd project
pip install -r requirements.txt
```

查看 `requirements.txt` 内容：

```txt
fastapi>=0.100.0        # Web 框架
uvicorn[standard]       # ASGI 服务器
sqlalchemy>=2.0.0       # ORM
aiosqlite               # 异步 SQLite 驱动
pydantic-settings       # 配置管理
httpx                   # 异步 HTTP 客户端
```

### 步骤 4：配置环境变量

```bash
# 复制配置模板
cp .env.example .env

# 编辑 .env 文件，填入你的 LLM API Key
```

`.env` 文件内容：

```env
# LLM API 配置
LLM_API_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=你的API密钥
LLM_MODEL=qwen-turbo

# 数据库配置
DATABASE_URL=sqlite+aiosqlite:///./ai_agent.db
```

### 步骤 5：启动项目

```bash
uvicorn app.main:app --reload
```

看到以下输出表示启动成功：

```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Application startup complete.
```

---

## 🧪 验证安装

### 访问 Web 界面

打开浏览器访问：`http://localhost:8000`

### 访问 API 文档

FastAPI 自动生成交互式 API 文档：

| 地址 | 说明 |
|------|------|
| http://localhost:8000/docs | Swagger UI（可直接测试 API）|
| http://localhost:8000/redoc | ReDoc（更适合阅读）|

### 测试健康检查接口

```bash
curl http://localhost:8000/health
```

预期响应：

```json
{
  "success": true,
  "data": {"status": "healthy"},
  "error": null
}
```

---

## 📝 本课小结

| 知识点 | 掌握程度 |
|--------|---------|
| 理解 AI Agent 平台的功能 | ☐ |
| 理解分层架构（Router-Service-Repository）| ☐ |
| 会创建 Python 虚拟环境 | ☐ |
| 会安装项目依赖 | ☐ |
| 能成功启动项目 | ☐ |
| 能访问 Swagger 文档 | ☐ |

---

## 🔜 下一课预告

**第02课：FastAPI 入门** - 学习 FastAPI 的核心概念，创建第一个 API 接口。
